import React  from 'react';
import Header from './header';

export default function About(props) {
    return (
        <>
            <Header {...props} />
            <p>Need to build this page...</p>
        </>
    )
}
